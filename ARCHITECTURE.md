# Architecture — مسجد المغفرة

## القرار التقني

- **Flutter / Dart**: تطبيق Android حقيقي بواجهة واحدة متجاوبة للهواتف والأجهزة اللوحية.
- **Presentation + Application + Data**: فصل الواجهات عن حالة التطبيق، التخزين، التنزيلات، والبيانات.
- **ChangeNotifier + InheritedNotifier**: إدارة حالة خفيفة بلا توليد كود، مناسبة لنسخة أولى تعمل دون حساب.
- **SharedPreferencesAsync**: تفضيلات وقوائم صغيرة محلية. عند نمو قاعدة المحتوى يمكن نقل المستودعات إلى SQLite/Drift دون تغيير الشاشات.
- **pdfrx**: عرض PDF بشكل كسول مع التكبير، الانتقال للصفحة، التحديد، والحفظ.

## مسار البيانات

1. الشاشة تقرأ الحالة من `AppScope`.
2. `AppController` ينفذ تغييرات المفضلة والعلامات والتقدم والثيم.
3. `LocalStore` يحفظ الحالة محلياً بصيغة JSON صغيرة.
4. `BookDownloadService` يبث ملف PDF مباشرة إلى القرص ولا يحتفظ به كاملاً في الذاكرة.

## الوحدات

- `core/`: الثيم، التخزين، التنزيل، وتطبيع البحث العربي.
- `models/`: Book وSurah وContentEntry وReadingProgress.
- `data/`: بيانات تجريبية معلَّمة ومحتوى فهرس السور.
- `state/`: حالة التطبيق العامة.
- `features/`: الشاشات حسب المجال.
- `widgets/`: مكونات Premium قابلة لإعادة الاستخدام.

## إضافة محتوى موثق

1. ضع ملفات PDF داخل `assets/books/` أو أضف رابط HTTPS موثوقًا في `remoteUrl`.
2. حدّث الكتاب داخل `lib/data/mock_data.dart`.
3. استبدل الـ placeholder داخل مستودع المحتوى بعد المراجعة الشرعية والتحريرية.
4. شغّل الاختبارات ثم أنشئ حزمة Android.

